# https://nightlies.apache.org/flink/flink-kubernetes-operator-docs-main/docs/operations/helm/
  operatorServiceAccount.create: true
  watchNamespaces:
  operatorPod:
    env:
    
  metrics.port:

  defaultConfiguration:
    log4j-operator:
      properties:

    log4j-console:
      properties: